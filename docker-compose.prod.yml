services:
  frontend:
    image: ghcr.io/degiwo/frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - 8080:8080

  backend:
    image: ghcr.io/degiwo/backend:latest
    container_name: backend
    restart: unless-stopped
    ports:
      - 8000:8000
